
AUR に `ttf-hackgen`があるので、それをインストールする。

```bash
yay -S ttf-hackgen
```

次のコマンドでインストールされてることが確認できる
```bash
fc-list | grep HackGen
```

Ghostty を使っているので、`.config/ghostty/config`を修正する

```
font-family = "HackGen35 Console"
```

反映されてるのか・・・？ｗ